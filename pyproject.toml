[tool.poetry]
name = "lesson27"
version = "0.1.0"
description = "27-Django"
authors = ["str4hex"]
license = "no"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
